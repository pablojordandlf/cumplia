[alembic]
script_location = %(here)s/../alembic
sqlalchemy.url =
version_locations = %(here)s/versions

# Database settings
# This URL will be overridden by the SQLAlchemy URL in core/config.py when the app runs.
# For Alembic to run standalone, you might need to set it here or via environment variables.
# Using a placeholder and assuming runtime will provide the actual DB URL.
# In a real scenario, you'd ensure DATABASE_URL is set in the environment where alembic is run.
